import { LogServicesInterceptor } from './log-services-interceptor';

describe('ErrorInterceptor', () => {
  it('should create an instance', () => {
    expect(new LogServicesInterceptor()).toBeTruthy();
  });
});
