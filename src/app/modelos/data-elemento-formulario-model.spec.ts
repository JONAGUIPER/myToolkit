import { DataElementoFormularioModel } from './data-elemento-formulario-model';

describe('DataElementoFormularioModel', () => {
  it('should create an instance', () => {
    expect(new DataElementoFormularioModel()).toBeTruthy();
  });
});
