import { ElementoFormularioBase } from './elemento-formulario-base';

describe('DataElementoFormularioModel', () => {
  it('should create an instance', () => {
    expect(new ElementoFormularioBase()).toBeTruthy();
  });
});
